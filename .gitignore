__pycache__/
*.py[cod]
.venv/
venv/
env/
.streamlit/secrets.toml
*.egg-info/
.DS_Store

# Local-only harness / preview artifacts
.claude/

# Taxonomy: the CSV is committed (used for AI tag generation); the source PDF
# is not needed by the app.
taxonomy/*.pdf

# Private: real submitter-name mapping; must never be committed
private/
